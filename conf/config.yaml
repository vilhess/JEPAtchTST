defaults:

  - encoder: config_encoder
  - pretraining: vicreg
  - finetuning: nyc_taxi
  
  - _self_  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

  
hydra:  
  output_subdir: null  
  run:  
    dir: .

